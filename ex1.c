#include<stdio.h>
int main(){
    int a, b, sum;
    printf("Enter the first number: ");
    scanf("%d", &a);
    printf("Entex the second number: ");
    scanf("%d", &b);
    sum = a + b;
    printf("The sum is: %d", sum);
    return 0;
}